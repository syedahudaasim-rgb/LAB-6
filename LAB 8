#include <stdio.h>

int main() {
    int pin = 0;

    while(pin != 5555) {
        printf("Enter PIN: ");
        scanf("%d", &pin);

        if(pin != 5555) {
            printf("Incorrect PIN\n");
        }
    }

    printf("Access Granted\n");

    return 0;
}
